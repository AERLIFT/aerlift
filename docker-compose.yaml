services:
  aerlift:
    build: .
    image: aerlift-app:latest
    container_name: aerlift
    env_file:
      - .env
    volumes:
      - .:/aerlift
      - "${DATA_DIR:-./data}:/aerlift/data"
    tty: true
    stdin_open: true
    command: ["snakemake", "--cores", "1"]